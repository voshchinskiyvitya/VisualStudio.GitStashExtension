<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tfc="clr-namespace:Microsoft.TeamFoundation.Controls.WPF;assembly=Microsoft.TeamFoundation.Controls"
                    xmlns:models="clr-namespace:VisualStudio.GitStashExtension.Models">

    <SolidColorBrush x:Key="ListViewItemBackgroundColor" Color="Red"/>

    <SolidColorBrush x:Key="ListViewItemHoverBackgroundColor" Color="#FDF4BF"/>

    <SolidColorBrush x:Key="ListViewItemSelectedBackgroundColor" Color="#007ACC"/>


    <Style x:Key="StashListItemStyle" TargetType="{x:Type ListBoxItem}">
        <Style.Setters>
            <Setter Property="Background" Value="{StaticResource ListViewItemBackgroundColor}"></Setter>
        </Style.Setters>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ListViewItemHoverBackgroundColor}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Control.Background" Value="{StaticResource ListViewItemSelectedBackgroundColor}"/>
            </Trigger>
        </Style.Triggers>
        <Style.Resources>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="LightYellow"/>
        </Style.Resources>
    </Style>

    <DataTemplate x:Key="StashListItemTemplate" DataType="models:Stash">
        <TextBlock Text="{Binding Message}"></TextBlock>
    </DataTemplate>

</ResourceDictionary>