<UserControl x:Class="VisualStudio.GitStashExtension.VS.UI.StashInfoPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualStudio.GitStashExtension.VS.UI"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             HorizontalContentAlignment="Stretch"
             VerticalContentAlignment="Stretch"
             Height="auto"
             Width="auto">
    <Grid Margin="15 10 15 10">
        <TextBlock Text="Stash #" FontSize="20" FontWeight="Bold"></TextBlock>
        <TextBlock Text="{Binding Stash.Id}" FontSize="20" FontWeight="Bold" Margin="70 0 0 60"></TextBlock>
        <TextBlock Text="{Binding Stash.Message}" Margin="0 30 0 30" FontSize="18"></TextBlock>
        <TextBlock Text="On branch:   " Margin="0 60 0 0"></TextBlock>
        <TextBlock Text="{Binding Stash.BranchName}" Margin="60 60 0 0"></TextBlock>
    </Grid>
</UserControl>
